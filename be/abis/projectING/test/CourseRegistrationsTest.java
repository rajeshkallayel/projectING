package be.abis.projectING.test;

import be.abis.projectING.model.Course;
import be.abis.projectING.repository.CoursesRegistrationsRepository;
import org.junit.jupiter.api.Test;

public class CourseRegistrationsTest {
    /*public static void main(String[] args) {
        CoursesRegistrationsRepository coursesRegistrationsRepository = new CoursesRegistrationsRepository();
        int counter = 0;
        for(Course c:coursesRegistrationsRepository.getCourseList()){
            System.out.print(c);
            System.out.printf("Total price for the %s course's orders is: %.2f%n", coursesRegistrationsRepository.getCourseList().get(counter).getName(), coursesRegistrationsRepository.getCourseList().get(counter).getTotalPriceForCourse());
            counter++;
        }
    }*/
}
